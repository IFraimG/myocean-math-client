import React from "react"
import Alert from '@material-ui/lab/Alert';

const NotFound = () => {
  return <Alert style={{ marginTop: "50px" }} severity="error">Такой страницы не существует !</Alert>
}

export default NotFound