import { colorTheme } from './../../theme/colors';
import { makeStyles } from "@material-ui/core/styles"

export const fr_stylesInput = makeStyles({
  tasks: {
    maxWidth: "400px",
    margin: "30px 0"
  }
})